html
<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Bảng Điều Khiển Key Kích Hoạt</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <div class="container">
        <header>
            <h1>Bảng Điều Khiển Key</h1>
        </header>

        <div class="control-panel">
            <!-- Form tạo key -->
            <div class="card">
                <h2>Tạo Key Mới</h2>
                <form action="/api/keys" method="POST" class="form-grid">
                    <div class="form-group">
                        <label for="count">Số lượng:</label>
                        <input type="number" id="count" name="count" value="1" min="1" max="100">
                    </div>
                    <div class="form-group">
                        <label for="expires_at">Ngày hết hạn:</label>
                        <input type="date" id="expires_at" name="expires_at" title="Để trống là vĩnh viễn">
                    </div>
                    <button type="submit" class="btn btn-primary">Tạo</button>
                </form>
            </div>

            <!-- Form lọc và tìm kiếm -->
            <div class="card">
                <h2>Lọc và Tìm Kiếm</h2>
                <div class="form-grid">
                    <div class="form-group">
                        <label for="filter-status">Trạng thái:</label>
                        <select id="filter-status">
                            <option value="">Tất cả</option>
                            <option value="unused">Chưa dùng</option>
                            <option value="used">Đã dùng</option>
                            <option value="locked">Bị khóa</option>
                            <option value="expired">Hết hạn</option>
                        </select>
                    </div>
                    <div class="form-group">
                        <label for="search-input">Tìm kiếm key:</label>
                        <input type="search" id="search-input" placeholder="Nhập một phần của key...">
                    </div>
                </div>
            </div>
        </div>

        <!-- Bảng dữ liệu -->
        <div class="table-container">
            <table id="key-table">
                <thead>
                    <tr>
                        <th>ID</th>
                        <th>Key Kích Hoạt</th>
                        <th>Trạng Thái</th>
                        <th>Thông Tin KH</th>
                        <th>Lượt Kích Hoạt</th>
                        <th>Ngày Hết Hạn</th>
                        <th>Hành Động</th>
                    </tr>
                </thead>
                <tbody id="key-table-body">
                    <!-- Dữ liệu sẽ được JavaScript chèn vào đây -->
                    <tr><td colspan="7" class="loading-text">Đang tải dữ liệu...</td></tr>
                </tbody>
            </table>
        </div>
    </div>

    <script src="/js/main.js"></script>
</body>
</html>