<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Bảng Điều Khiển Key Kích Hoạt</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <div class="container">
        <header>
            <h1>Bảng Điều Khiển Key</h1>
            <div>
                <button id="manage-notifications-btn" class="btn btn-info">Quản lý Thông báo</button>
                <a href="/logout" id="logout-button" class="btn btn-secondary">Đăng xuất</a>
            </div>
        </header>

        <div class="control-panel">
            <div class="card">
                <h2>Tạo Key Mới</h2>
                <form id="create-key-form" class="form-grid">
                    <div class="form-group">
                        <label for="count">Số lượng:</label>
                        <input type="number" id="count" name="count" value="1" min="1" max="100">
                    </div>
                    <div class="form-group">
                        <label for="expires_at">Ngày hết hạn:</label>
                        <input type="date" id="expires_at" name="expires_at" title="Để trống là vĩnh viễn">
                    </div>
                    <div class="form-group form-group-full">
                        <label for="notes">Ghi chú (tùy chọn):</label>
                        <input type="text" id="notes" name="notes" placeholder="VD: Key cho sự kiện X, tặng khách hàng Y...">
                    </div>
                    <div class="form-group form-group-full" style="flex-direction: row; align-items: center; gap: 10px;">
                        <input type="checkbox" id="is_trial_key" name="is_trial_key" style="width: auto;">
                        <label for="is_trial_key" style="margin-bottom: 0;">Tạo key dùng thử (cho phép nhiều người cùng sử dụng)</label>
                    </div>
                    <button type="submit" class="btn btn-primary form-group-full">Tạo Key</button>
                </form>
            </div>

            <div class="card">
                <h2>Lọc và Tìm Kiếm</h2>
                <div class="form-grid">
                    <div class="form-group">
                        <label for="filter-status">Trạng thái:</label>
                        <select id="filter-status">
                            <option value="">Tất cả</option>
                            <option value="unused">Chưa dùng</option>
                            <option value="used">Đã dùng</option>
                            <option value="locked">Bị khóa (thủ công)</option>
                            <option value="forced">Bị cưỡng chế</option>
                            <option value="expired">Hết hạn</option>
                        </select>
                    </div>
                    <div class="form-group">
                        <label for="search-input">Tìm kiếm (Key/FP):</label>
                        <input type="search" id="search-input" placeholder="Nhập từ khóa...">
                    </div>
                    <div class="form-group form-group-full">
                        <label for="filter-notes">Lọc theo Ghi chú:</label>
                        <input type="search" id="filter-notes" placeholder="Nhập nội dung ghi chú...">
                    </div>
                </div>
            </div>
        </div>

        <div id="bulk-action-bar" class="bulk-action-bar">
            <span id="selection-count">Đã chọn: 0</span>
            <select id="bulk-action-select">
                <option value="">-- Chọn hành động --</option>
                <option value="export">Xuất các key đã chọn</option>
                <option value="lock">Khóa các key đã chọn</option>
                <option value="unlock">Mở khóa các key đã chọn</option>
                <option value="delete">Xóa các key đã chọn</option>
            </select>
            <button id="bulk-action-execute" class="btn btn-danger">Thực Hiện</button>
        </div>

        <div class="table-container">
            <table id="key-table">
                <thead>
                    <tr>
                        <th><input type="checkbox" id="select-all-keys"></th>
                        <th class="sortable" data-sort="created_at">Ngày Tạo</th>
                        <th class="sortable" data-sort="activation_key">Key Kích Hoạt</th>
                        <th class="sortable" data-sort="is_activated">Trạng Thái</th>
                        <th class="sortable" data-sort="expires_at">Hạn Dùng</th>
                        <th class="sortable" data-sort="notes">Ghi Chú</th>
                        <th class="sortable" data-sort="device_change_count">FP & Lần Đổi</th>
                        <th class="sortable" data-sort="activation_count">Lượt</th>
                        <th class="sortable" data-sort="last_heartbeat">Heartbeat Cuối</th>
                        <th>Hành Động</th>
                    </tr>
                </thead>
                <tbody id="key-table-body">
                    <tr><td colspan="10" class="loading-text">Đang tải dữ liệu...</td></tr>
                </tbody>
            </table>
        </div>
    </div>

    <div id="history-modal" class="modal">
        <div class="modal-content">
            <span class="close-button">&times;</span>
            <h2>Lịch Sử Kích Hoạt</h2>
            <div id="history-content"></div>
        </div>
    </div>

    <div id="password-modal" class="modal">
        <div class="modal-content">
            <span class="close-button">&times;</span>
            <h2>Xác Nhận Hành Động</h2>
            <p>Vui lòng nhập mật khẩu quản trị để xác nhận hành động nguy hiểm này.</p>
            <input type="password" id="password-confirm-input" placeholder="Nhập mật khẩu...">
            <button id="password-confirm-button" class="btn btn-danger">Xác Nhận</button>
        </div>
    </div>

    <div id="notifications-modal" class="modal">
        <div class="modal-content">
            <span class="close-button">&times;</span>
            <h2>Quản lý Thông báo</h2>
            <p>Gửi một thông báo đến tất cả client đang hoạt động. Chỉ thông báo mới nhất được kích hoạt.</p>
            <form id="notification-form">
                <textarea id="notification-message" placeholder="Nhập nội dung thông báo..."></textarea>
                <button type="submit" class="btn btn-primary">Gửi Thông Báo Mới</button>
            </form>
            <h3>Lịch sử thông báo</h3>
            <ul id="notification-history" class="history-list"></ul>
        </div>
    </div>

    <div id="export-modal" class="modal">
        <div class="modal-content">
            <span class="close-button">&times;</span>
            <h2>Xuất Key Đã Chọn</h2>
            <p>Đây là danh sách các key bạn đã chọn. Nhấn nút bên dưới để sao chép tất cả.</p>
            <textarea id="export-textarea" readonly></textarea>
            <button id="copy-export-button" class="btn btn-primary">Sao chép tất cả</button>
        </div>
    </div>

    <script src="/js/main.js"></script>
</body>
</html>